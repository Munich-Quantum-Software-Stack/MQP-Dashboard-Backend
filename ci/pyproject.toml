[project]
name = "bqp-dashboard-backend-ci"
version = "0.0.0"
description = "CI-only tool environment"
requires-python = ">=3.10,<3.12"
dependencies = []

[dependency-groups]
dev = [
  "isort",
  "mypy",
  "nox",
  "pre-commit",
  "pytest>=7.4.0",
  "pytest-env>=0.8.2",
]
